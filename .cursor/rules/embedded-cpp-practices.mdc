---
description: 嵌入式C++开发规范
alwaysApply: true
---

# 嵌入式C++开发规范

## 1. CRTP（奇异递归模板模式）

**作用**：编译时多态，零运行时开销

**基本语法**：
- 基类模板接受派生类为模板参数：`template <typename Derived> class Base`
- 基类通过 `static_cast<Derived&>(*this)` 调用派生类方法
- 派生类实现方法使用 `Impl` 后缀（如 `SetPWMImpl`）

**为什么**：避免虚函数的vtable查找开销，适合资源受限的嵌入式系统

---

## 2. 模板与编译时编程

**constexpr 使用**：
- 使用 `constexpr` 定义编译时常量（存储在Flash而非RAM）
- 编译时计算：`constexpr int factorial(int n)`
- 常量数据：`static constexpr uint8_t kLookupTable[] = {...}`

**模板参数配置**：
- 用模板参数实现类型安全的编译时配置
- 避免运行时配置占用RAM

---

## 3. 内存管理

**避免动态分配**：
- 优先使用栈分配或静态分配
- 禁止使用 `new`/`delete`/`malloc`/`free`
- 原因：可能失败、内存碎片化、不确定性

**成员变量初始化**：
- 使用类内初始化：`int value_{42}`
- 避免在构造函数中赋值（默认构造再赋值有额外开销）

**constexpr 数据存储**：
- 大数据表用 `constexpr` 存储在Flash而非RAM

---

## 4. 避免运行时开销

**禁用的C++特性**：
- 虚函数（除非确实需要）→ vtable查找开销
- RTTI（`typeid`、`dynamic_cast`）→ 代码膨胀
- 异常（`try`/`catch`）→ 代码膨胀和性能损失
- 动态容器（`std::vector`、`std::map`）→ 动态分配

**推荐的替代方案**：
- 用CRTP替代虚函数
- 用编译时类型检查替代RTTI
- 用错误码替代异常
- 用固定大小数组或 `std::array` 替代动态容器

---

## 5. 类型安全

**强类型枚举**：
- 使用 `enum class`（类型安全、不污染命名空间）
- 避免老式 `enum`（易冲突、隐式转换为int）

**类型别名**：
- 使用 `using`（更清晰、支持模板）
- 避免 `typedef`（老式语法）

---

## 6. 并发与中断安全

**volatile 修饰符**：
- 中断或多线程修改的变量必须用 `volatile`
- 防止编译器优化掉必要的读写操作

**临界区保护**：
- 多字节操作需要关中断保护
- 使用 `noInterrupts()` / `interrupts()` 实现原子操作

---

## 7. 初始化模式

**Config 结构体模式**：
- 使用 `Config` 结构体集中配置参数
- 支持默认值和指定初始化器（C++20）
- 避免多参数构造函数（易出错、不灵活）

---

## 8. 错误处理

**错误码模式**：
- 返回 `enum class Error` 类型的错误码
- 使用 `CHECK` 宏传播错误
- 避免异常（代码膨胀、不确定性）

**防御性编程**：
- 参数校验和限幅
- 空指针检查
- 数组边界检查

---

## 9. 内联与优化

**合理使用 inline**：
- 小函数用 `inline` 避免函数调用开销
- 类内定义的函数自动 `inline`

**编译器优化提示**：
- 使用 `constexpr` 帮助编译器优化
- 编译时计算常量表达式

---

## 10. 编译环境 

**C++版本**：
- gnu++17

---

## 检查清单

开发时确认：

- [ ] 使用CRTP替代虚函数实现多态
- [ ] 使用constexpr定义编译时常量
- [ ] 避免动态内存分配（new/delete/malloc）
- [ ] 使用enum class而非enum
- [ ] 中断共享变量用volatile声明
- [ ] 禁用RTTI和异常（编译选项：-fno-rtti -fno-exceptions）
- [ ] 返回错误码而非抛出异常
- [ ] 大数据表用constexpr存储在Flash
- [ ] Config结构体模式初始化
- [ ] 临界区使用中断保护

---

**说明**：本规范适用于资源受限的嵌入式系统，优先考虑性能、内存效率和确定性行为。
